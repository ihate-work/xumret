name: Check

on:
  workflow_call:
  workflow_dispatch:
  pull_request:
  push:
    branches: [master]

jobs:
  check:
    name: Run tests
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v6

      - name: Seed Makefile.var from template
        run: cp Makefile.var.template Makefile.var

      - run: PYTHON_VER=${{ matrix.python-version }} make deps

      - run: make typecheck

      - run: make test-cov

      - name: Build production webui
        if: matrix.python-version == '3.13'
        run: make webui-prod-build

      - name: Upload coverage to Codecov
        if: matrix.python-version == '3.13'
        uses: codecov/codecov-action@v5
        with:
          files: coverage.xml
          use_oidc: true
